Atividade 1:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
				OrderDetailID AS 'OrdemDetalhadaID'

FROM orderdetails
WHERE Quantity < 10 
OR ProductID > 20
ORDER BY OrderDetailID;

Atividade 2:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
				OrderID AS 'OrdemID'

FROM orderdetails
WHERE OrderID LIKE 100
OR ProductID%5 = 0
ORDER BY Quantity;

Atividade 3:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
				OrderDetailID AS 'OrdemDetalhadaID',
						OrderID AS 'IDordem'

FROM orderdetails
WHERE OrderDetailID > 50
OR Quantity < 20
ORDER BY OrderID;

Atividade 4:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'

FROM orderdetails
WHERE ProductID < 15
OR OrderID LIKE '%0'
ORDER BY Quantity;

Atividade 5:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'

FROM orderdetails
WHERE Quantity BETWEEN 20 AND 50
OR OrderDetailID%3 = 0
ORDER BY OrderID;

Atividade 6:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'

FROM orderdetails
WHERE OrderID > 200
OR ProductID LIKE '%7'
ORDER BY Quantity;

Atividade 7:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
									OrderDetailID AS 'ORDERMDETALHADAID'

FROM orderdetails
WHERE OrderID < 50
OR Quantity%4 = 0
ORDER BY OrderDetailID;

Atividade 8:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'

FROM orderdetails
WHERE Quantity BETWEEN 10 AND 30
OR ProductID LIKE '8%'
ORDER BY OrderID;

Atvidade 9:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
								OrderDetailID AS 'ORDERMDETALHADAID'

FROM orderdetails
WHERE OrderDetailID LIKE '%5' 
OR ProductID > 25
ORDER BY Quantity;

Atividade 10:

SELECT Quantity AS 'Quantidade',
		
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'

FROM orderdetails
WHERE OrderID LIKE 150
OR Quantity < 15
ORDER BY OrderDetailID;

Atividade 11:

SELECT Quantity AS 'Quantidade',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'

FROM orderdetails
WHERE OrderID LIKE 250
OR Quantity < 50
ORDER BY OrderDetailID;

Atividade 12:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'

FROM orderdetails
WHERE ProductID > 30
OR OrderID BETWEEN 100 AND 300
ORDER BY Quantity;

Atividade 13:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'

FROM orderdetails
WHERE OrderDetailID < 15
OR Quantity%5 = 0
ORDER BY ProductID;

Atividade 14:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								

FROM orderdetails
WHERE ProductID < 12
OR OrderID LIKE '%9'
ORDER BY Quantity;

Atividade 15:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								

FROM orderdetails
WHERE Quantity > 25
OR OrderID%4 = 0
ORDER BY OrderDetailID;

Atividade 16:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								

FROM orderdetails
WHERE ProductID LIKE 45
OR OrderID < 200
ORDER BY Quantity;

Atividade 17:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								

FROM orderdetails
WHERE OrderDetailID LIKE 50
OR Quantity > 30
ORDER BY OrderID;

Atividade 18:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								

FROM orderdetails
WHERE OrderDetailID%6 = 0
OR Quantity < 5
ORDER BY ProductID;

Atividade 19:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								

FROM orderdetails
WHERE OrderID > 10
OR Quantity BETWEEN 10 AND 20
ORDER BY Quantity;

Atividade 20:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								

FROM orderdetails
WHERE ProductID > 15
OR OrderDetailID LIKE '%2'
ORDER BY OrderID;

Atividade 21:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								

FROM orderdetails
WHERE ProductID > 50
OR Quantity < 5
ORDER BY OrderID;

Atividade 22:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								

FROM orderdetails
WHERE OrderDetailID LIKE '%3'
OR Quantity BETWEEN 10 AND 15
ORDER BY ProductID;

Atividade 23:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								

FROM orderdetails
WHERE OrderID%7 = 0
OR Quantity LIKE '25'
ORDER BY Quantity;

Atividade 24:

SELECT ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								

FROM orderdetails
WHERE OrderDetailID < 10
OR ProductID BETWEEN  15 AND 20
ORDER BY OrderID;

Atividade 25:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								

FROM orderdetails
WHERE OrderID > 300
OR Quantity%2 = 0
ORDER BY OrderDetailID;

Ativiade 26:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE ProductID%9 = 0
OR Quantity BETWEEN 1 AND 10
ORDER BY OrderID;

Atividade 27:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								

FROM orderdetails
WHERE OrderDetailID LIKE '100'
OR ProductID > 25
ORDER BY Quantity;

Atividade 28:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE Quantity LIKE '50'
OR OrderID LIKE '%8'
ORDER BY ProductID;

Atividade 29:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								    OrderDetailID AS 'ORDERMDETALHADAID'
								
								

FROM orderdetails
WHERE ProductID < 20
OR Quantity > 40
ORDER BY OrderDetailID;

Atividade 30:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE OrderDetailID%5 = 0
OR Quantity%2 = 1
ORDER BY OrderID;

Atividade 31:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE OrderDetailID < 5
OR Quantity > 10
ORDER BY ProductID;

Atividade 32:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE ProductID%2 = 0
OR OrderID > 500
ORDER BY Quantity;

Atividade 33:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE OrderDetailID LIKE '15'
OR Quantity < 20
ORDER BY OrderID;

Atividade 34:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								  OrderDetailID AS 'ORDERMDETALHADAID'
								
								

FROM orderdetails
WHERE ProductID > 50
OR Quantity%2 = 1
ORDER BY OrderDetailID;

Atividade 35:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE OrderID LIKE '%0'
OR ProductID BETWEEN 30 AND 40
ORDER BY  Quantity;

Atividade 36:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE Quantity > 50
OR OrderID < 100
ORDER BY ProductID;


Atividade 37:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								
								

FROM orderdetails
WHERE OrderDetailID%10 = 0
OR Quantity BETWEEN 5 AND 15
ORDER BY OrderID;

Atividade 38:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								
								

FROM orderdetails
WHERE Quantity LIKE '100'
OR ProductID < 5
ORDER BY OrderDetailID;

Atividade 39:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem'
								
								
								

FROM orderdetails
WHERE OrderID%6 = 0
OR ProductID > 40
ORDER BY Quantity;

Atividade 40:

SELECT Quantity AS 'Quantidade',
			ProductID AS 'IDproduto',
						OrderID AS 'IDordem',
								OrderDetailID AS 'ORDERMDETALHADAID'
								
								
								

FROM orderdetails
WHERE OrderDetailID < 20
OR Quantity > 30
ORDER BY OrderID;

Atividade 41:
SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (50, 100, 150)
ORDER BY Quantity ASC;

Atividade 42:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE  ProductID IN (2, 4, 6, 8)
ORDER BY OrderDetailID ASC;

Atividade 43:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE  Quantity IN (10, 20, 30)
ORDER BY OrderID ASC;

Atividade 44:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (5, 10, 15, 20)
ORDER BY Quantity ASC;

Atividade 45:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (200, 300, 400)
ORDER BY ProductID ASC;

Atividade 46:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (12, 14, 16)
ORDER BY Quantity ASC;

Atividade 47:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (25, 50, 75, 100)
ORDER BY OrderID ASC;


Atividade 48:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (100, 200, 300)
ORDER BY ProductID ASC;

Atividade 49:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (5, 10, 15, 20)
ORDER BY Quantity ASC;

Atividade 50:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (1, 2, 3, 4, 5)
ORDER BY OrderID ASC;

Atividade 51:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (250, 350, 450)
ORDER BY Quantity ASC;

Atividade 52:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (11, 21, 31)
ORDER BY OrderDetailID ASC;

Atividade 53:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (40, 50, 60)
ORDER BY OrderID ASC;

Atividade 54:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (150, 250, 350)
ORDER BY ProductID ASC;

Atividade 55:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (7, 14, 21)
ORDER BY Quantity ASC;

Atividade 56:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (50, 100, 200)
ORDER BY OrderDetailID ASC

Atividade 57:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (20, 25, 30)
ORDER BY ProductID ASC;

Atividade 58:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (10, 20, 30)
ORDER BY Quantity ASC;


Atividade 59:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (100, 200, 300)
ORDER BY Quantity ASC;

Atividade 60:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (5, 10, 15)
ORDER BY OrderID ASC;

Atividade 61:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (25, 50, 75, 100)
ORDER BY Quantity ASC;

Atividade 62:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (9, 18, 27)
ORDER BY OrderID ASC;

Atividade 63:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (12, 14, 16)
ORDER BY ProductID ASC;

Atividade 64:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (400, 500, 600)
ORDER BY Quantity ASC;

Atividade 65:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (20, 40, 60, 80)
ORDER BY OrderID ASC;

Atividade 66:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (5, 10, 20)
ORDER BY Quantity ASC;

Atividade 67:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (8, 16, 24, 32)
ORDER BY OrderDetailID ASC;

Atividade 68:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (300, 400, 500)
ORDER BY ProductID ASC;

Atividade 69:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (15, 30, 45)
ORDER BY OrderID ASC;

Atividade 70:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (50, 150, 250)
ORDER BY Quantity ASC;

Atividade 71:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (100, 200, 300, 400)
ORDER BY Quantity ASC;

Atividade 72:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (10, 20, 30, 40)
ORDER BY OrderID ASC;

Atividade 73:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (5, 10, 15, 20)
ORDER BY ProductID ASC;

Atividade 74:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (50, 150, 250, 350)
ORDER BY Quantity ASC;

Atividade 75:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (1, 3, 5, 7)
ORDER BY OrderID ASC;

Atividade 76:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE ProductID IN (13, 26, 39)
ORDER BY Quantity ASC;

Atividade 77:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (22, 44, 66, 88)
ORDER BY OrderDetailID ASC;

Atividade 78:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderID IN (90, 180, 270)
ORDER BY ProductID ASC;

Atividade 79:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE Quantity IN (8, 16, 24, 32)
ORDER BY OrderID ASC;

Atividade 80:

SELECT
    OrderDetailID,
    OrderID,
    ProductID,
    Quantity
FROM OrderDetails
WHERE OrderDetailID IN (101, 201, 301, 401)
ORDER BY Quantity ASC;
